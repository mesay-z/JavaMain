/*package dataStracture1;

import java.util.Iterator;


//Demonstrate how to use Queue that includes add,peek,remove,pool elements.
//* Use For Each loop and while loop with Iterator to retrieve data.
public class primaryQueueUse {
    public static void main(String[] args) {
        primaryQueue<Integer> qu = new primaryQueue<Integer>(16);
        qu.add(3);
        qu.add(7);
        qu.add(2);
        qu.add(4);
        qu.add(1);
        qu.add(5);

        Integer head = qu.peek();
        head = qu.poll();
        head = qu.peek();
        head=qu.element();
        head=qu.remove();

        System.out.println("Current value of head : " + head);
        System.out.println("size of PriorityQueue : " + qu.size());


        Iterator<Integer> itr = qu.iterator();
        System.out.println("Iterating over Queue");

        while (itr.hasNext()) {
            System.out.println(itr.next());
        }
    }
}*/

